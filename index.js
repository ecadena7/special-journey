const inquirer = require("inquirer");
const fs = require('fs')
const Manager = require("./lib/Manager");
const Intern = require("./lib/Intern");
const Engineer = require("./lib/Engineer");



// TODO writeToFile this will use the 'fs' found in line 2


// TODO generate an HTML ← function

// TODO function to add an intern

// TODO function to add an engineer

// TODO function to add an employee(non-mgmt)

// TODO function to add an employee(mgmt)

// TODO function to call init();